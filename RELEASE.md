# Release Guide

Руководство по созданию релизов Voice to Text с автоматическими обновлениями.

## 🔧 Первоначальная настройка (один раз)

### 1. Генерация ключей для подписи обновлений

```bash
cd /path/to/voice-to-text
pnpm tauri signer generate -w .tauri/voice-to-text.key
```

При запросе пароля можно нажать Enter (оставить пустым) или ввести пароль для дополнительной защиты.

Будут созданы два файла:
- `.tauri/voice-to-text.key` - **приватный ключ** (НЕ КОММИТИТЬ!)
- `.tauri/voice-to-text.pub` - публичный ключ

### 2. Обновление tauri.conf.json

Скопируйте содержимое публичного ключа:

```bash
cat .tauri/voice-to-text.pub
```

Замените в `src-tauri/tauri.conf.json`:

```json
{
  "plugins": {
    "updater": {
      "pubkey": "ВСТАВЬТЕ_СЮДА_СОДЕРЖИМОЕ_voice-to-text.pub"
    }
  }
}
```

### 3. Настройка GitHub Secrets

Перейдите в настройки репозитория:
**Settings → Secrets and variables → Actions → New repository secret**

Создайте два секрета:

**Secret 1: TAURI_SIGNING_PRIVATE_KEY**
```bash
# Скопируйте содержимое приватного ключа
cat .tauri/voice-to-text.key
```
Вставьте его как значение секрета.

**Secret 2: TAURI_SIGNING_PRIVATE_KEY_PASSWORD** (опционально)
- Если вы установили пароль при генерации ключей - добавьте его сюда
- Если пароля нет - оставьте пустым или не создавайте этот секрет

### 4. Настройка Permissions для GitHub Actions

**Settings → Actions → General → Workflow permissions:**
- Выберите **"Read and write permissions"**
- Сохраните изменения

Без этого GitHub Actions не сможет создавать релизы.

## 🚀 Создание релиза

### Способ 1: Через Git Tag (рекомендуется)

```bash
# 1. Обновите версию в нужных файлах
# - src-tauri/tauri.conf.json
# - package.json
# - src-tauri/Cargo.toml

# 2. Закоммитьте изменения
git add .
git commit -m "chore: bump version to 0.2.0"

# 3. Создайте и запушьте тег
git tag v0.2.0
git push origin main
git push origin v0.2.0
```

GitHub Actions автоматически:
- Соберет приложение для всех платформ
- Подпишет обновления
- Создаст GitHub Release
- Сгенерирует `latest.json` для автообновлений

### Способ 2: Ручной запуск через GitHub UI

1. Перейдите на вкладку **Actions** в репозитории
2. Выберите **Release** workflow
3. Нажмите **Run workflow**
4. Выберите ветку и версию
5. Запустите

## 📦 Что будет создано

После успешной сборки в GitHub Release появятся:

### macOS
- `Voice-to-Text_0.2.0_x64.dmg` (Intel)
- `Voice-to-Text_0.2.0_aarch64.dmg` (Apple Silicon)
- `.dmg.sig` файлы с подписями

### Windows
- `Voice-to-Text_0.2.0_x64_en-US.msi`
- `Voice-to-Text_0.2.0_x64-setup.exe` (NSIS)
- `.msi.zip` и `.msi.zip.sig`

### Linux
- `voice-to-text_0.2.0_amd64.deb` (Debian/Ubuntu)
- `voice-to-text_0.2.0_amd64.AppImage` (универсальный)
- `.deb.tar.gz` и `.deb.tar.gz.sig`

### Файл обновлений
- `latest.json` - метаданные для автообновлений

## 🔄 Как работают автообновления

1. **Фоновая проверка**: Приложение проверяет обновления каждые 6 часов
2. **Источник**: GitHub Releases (endpoint в `tauri.conf.json`)
3. **Проверка подписи**: Используется публичный ключ из конфига
4. **Уведомление**: Показывается диалог с релиз-нотами
5. **Установка**: Скачивание → проверка подписи → установка → перезапуск

## 🛡️ Безопасность

- ✅ Все обновления криптографически подписаны
- ✅ Приватный ключ хранится только в GitHub Secrets
- ✅ Подпись проверяется перед установкой
- ✅ Невозможно установить поддельное обновление

## 🐛 Troubleshooting

### "Resource not accessible by integration"
**Решение:** Включите "Read and write permissions" в Settings → Actions → General

### "Failed to verify signature"
**Решение:** Убедитесь что публичный ключ в `tauri.conf.json` совпадает с `.tauri/voice-to-text.pub`

### Workflow не запускается при push тега
**Решение:** Убедитесь что тег начинается с `v` (например `v0.1.0`, а не `0.1.0`)

## 📝 Checklist перед релизом

- [ ] Обновить версию в `src-tauri/tauri.conf.json`
- [ ] Обновить версию в `package.json`
- [ ] Обновить версию в `src-tauri/Cargo.toml`
- [ ] Обновить CHANGELOG.md с описанием изменений
- [ ] Создать и запушить git tag
- [ ] Дождаться успешной сборки в GitHub Actions
- [ ] Проверить что релиз создан с всеми артефактами
- [ ] Протестировать автообновление на одной из платформ
